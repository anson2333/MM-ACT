compute_environment: LOCAL_MACHINE
deepspeed_config:
  gradient_accumulation_steps: 2
  gradient_clipping: 1.0
  offload_optimizer_device: cpu       # 优化器状态放CPU
  offload_param_device: cpu           # 参数也放CPU
  zero3_init_flag: true               # ZeRO-3初始化
  zero_stage: 3                       # ← 关键：ZeRO-3
distributed_type: DEEPSPEED
downcast_bf16: 'no'
main_training_function: main
mixed_precision: bf16
num_machines: 1
num_processes: 4
rdzv_backend: static
same_network: true
use_cpu: false